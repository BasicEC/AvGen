namespace AvGen
{
    /// <summary>
    /// It is an enumeration of the hash types that are supported by <see cref="Hasher"/>.
    /// </summary>
    public enum HashType
    {
        Sha1,
        Sha256,
        Sha384,
        Sha512,
    }
}
